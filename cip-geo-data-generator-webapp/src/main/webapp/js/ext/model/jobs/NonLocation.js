Ext.define('DataGenerator.model.jobs.NonLocation', {
    extend: 'Ext.data.Model',

    fields: [
        { name: 'id', type: 'int' }
    ],

    schema: {
        namespace: 'DataGenerator.model.jobs'
    }
});
